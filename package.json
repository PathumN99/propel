{
  "name": "propel",
  "version": "1.0.0",
  "description": "Propel - Enterprise Presales AI",
  "private": true,
  "packageManager": "pnpm@10.18.1",
  "scripts": {
    "dev": "pnpm --filter propel-api dev",
    "build": "pnpm --filter propel-api build",
    "db:generate": "pnpm --filter database db:generate",
    "db:push": "pnpm --filter database db:push"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}
